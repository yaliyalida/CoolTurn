使用批量写入word文件功能时，可选择批量写入文本形式和批量写入表格形式两种功能。

1、批量写入文本形式：
需要提供模板文件路径。
模板文件设置规则：“{}”用这个标记填写位置，程序将输入的值依次填入这些位置，第一个“{}”为第一个字段，以此类推。

2、批量写入表格形式
需要提供模板文件路径。
模板文件设置规则：“{}”用这个标记填写位置，程序将输入的值依次填入这些位置，第一个“{}”为第一个字段，以此类推。

（其余操作根据程序运行提示）